export enum Palette {
    Accent = 'accent',
    Background = 'background',
    Primary = 'primary',
    Muted = 'muted',
    Secondary = 'secondary',
    Selection = 'selection',
    Text = 'text',
}
