export enum Palette {
    Accent = 'accent',
    Background = 'background',
    Highlight = 'highlight',
    Field = 'field',
    Primary = 'primary',
    Muted = 'muted',
    Secondary = 'secondary',
    Selection = 'selection',
    Text = 'text',
}
