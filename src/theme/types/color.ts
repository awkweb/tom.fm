export enum Palette {
    Accent = 'accent',
    Background = 'background',
    Field = 'field',
    Primary = 'primary',
    Muted = 'muted',
    Secondary = 'secondary',
    Selection = 'selection',
    Text = 'text',
}
