export enum Align {
    Left = 'left',
    Center = 'center',
    Right = 'right',
}

export enum Font {
    Body = 'body',
    Heading = 'heading',
    Monospace = 'monospace',
}

export enum LineHeight {
    Body = 'body',
    Heading = 'heading',
    Small = 'small',
}

export enum Size {
    Body = 'body',
    Heading = 'heading',
    Root = 'root',
    Small = 'small',
}

export enum Weight {
    Bold = 'bold',
    Medium = 500,
    Normal = 'normal',
}
