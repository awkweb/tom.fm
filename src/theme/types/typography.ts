export enum Font {
    Body = 'body',
    Heading = 'heading',
    Monospace = 'monospace',
}

export enum LineHeight {
    Body = 'body',
    Heading = 'heading',
}

export enum Size {
    Small = 'small',
    Body = 'body',
    Heading = 'heading',
}

export enum Weight {
    Bold = 'bold',
    Normal = 'normal',
}
