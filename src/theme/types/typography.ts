export enum Align {
    Left = 'left',
    Center = 'center',
    Right = 'right',
}

export enum Font {
    Body = 'body',
    Heading = 'heading',
    Monospace = 'monospace',
}

export enum LineHeight {
    Body = 'body',
    Heading = 'heading',
}

export enum Size {
    Body = 'body',
    Heading = 'heading',
    Root = 'root',
}

export enum Weight {
    Bold = 'bold',
    Normal = 'normal',
}
